Proceed with the previously approved plan: implement `auto-generate-pages` background edge function triggered by the LemonSqueezy webhook, add `INTERNAL_FUNCTION_SECRET`, update config.toml, and surface `pages_in_progress` state in the admin UI. No changes to the previously approved scope.